<UserControl x:Class="PizzaApp.CartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#FFF5F5F5" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Пустая корзина -->
        <Border Grid.Row="0"
            Background="White"
            CornerRadius="10"
            Padding="20"
            Margin="0,0,0,10"
            Visibility="Collapsed"
            x:Name="IsEmptyLb">
            <TextBlock Text="Ваша корзина пуста"
                   FontSize="16"
                   Foreground="Gray"
                   HorizontalAlignment="Center"/>
        </Border>

        <!-- Список товаров -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" MaxHeight="400">
            <ItemsControl ItemsSource="{Binding CartItems}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White"
                            CornerRadius="10"
                            Padding="15"
                            Margin="10"
                            BorderBrush="LightBlue"
                            BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Название + цена -->
                                <StackPanel>
                                    <TextBlock Text="{Binding ProductName}"
                                           FontSize="20"
                                           FontWeight="SemiBold"/>

                                    <TextBlock Text="{Binding TotalItemPrice, StringFormat={}{0} ₽}"
                                           FontSize="18"
                                           FontWeight="Bold"
                                           Foreground="#FF3C3C3C"
                                           Margin="0,5,0,0"/>
                                </StackPanel>

                                <!-- Управление -->
                                <StackPanel Grid.Column="1"
                                        Orientation="Horizontal"
                                        VerticalAlignment="Center">
                                    <Button Content="−"
                                        Width="28"
                                        Height="28"
                                        Margin="2" Click="MinusBt_Click"/>

                                    <TextBlock Text="{Binding Quantity}"
                                           FontSize="18"
                                           FontWeight="Bold"
                                           VerticalAlignment="Center"
                                           Margin="10,0"/>

                                    <Button Content="+"
                                        Width="28"
                                        Height="28"
                                        Margin="2" Click="PlusBt_Click"/>

                                    <Button Content="✕"
                                        Width="28"
                                        Height="28"
                                        Margin="10,2,2,2"
                                        Foreground="Red" Click="RemoveBt_Click" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Нижняя панель -->
        <Border Grid.Row="2"
            Background="White"
            Padding="15"
            CornerRadius="10"
            Margin="0,10,0,0">
            <Button Content="{Binding TotalCost}" ContentStringFormat="Оформить за {0} ₽" 
                Height="45"
                FontSize="16"
                Background="#FF3C3C3C"
                Foreground="White"
                BorderThickness="0"
                Cursor="Hand"
                x:Name="CreateOrderBt" Visibility="Collapsed" Click="CreateOrderBt_Click" />
        </Border>
    </Grid>
    
    <UserControl.Style>
        <Style TargetType="UserControl">
            <Style.Triggers>
                <Trigger Property="Visibility" Value="Visible">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                             From="0.0" To="1.0" Duration="0:0:0.3"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Style>
    
</UserControl>
