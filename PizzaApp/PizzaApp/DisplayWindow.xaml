<Window x:Class="PizzaApp.DisplayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PizzaApp"
        mc:Ignorable="d"
        Title="DisplayWindow" Height="450" Width="800">
    <Grid Background="Black">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" HorizontalAlignment="Center">
            <Label Content="Готовятся" FontSize="30" FontWeight="Bold" Foreground="Wheat"/>
            <ItemsControl ItemsSource="{Binding InProcess}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" CornerRadius="6" Padding="10" Margin="5" BorderBrush="LightBlue" BorderThickness="1">

                            <TextBlock Text="{Binding Name}" FontSize="20" FontWeight="Bold" Margin="10"/>

                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
        
        <StackPanel Grid.Column="1" HorizontalAlignment="Center">
            <Label Content="Готовы к выдаче" FontSize="30" FontWeight="Bold" Foreground="Wheat"/>
            <ItemsControl ItemsSource="{Binding Ready}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" CornerRadius="6" Padding="10" Margin="5" BorderBrush="LightBlue" BorderThickness="1">

                            <TextBlock Text="{Binding Name}" FontSize="20" FontWeight="Bold" Margin="10" HorizontalAlignment="Center"/>

                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>
</Window>
