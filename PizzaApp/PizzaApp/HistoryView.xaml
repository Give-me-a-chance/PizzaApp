<UserControl x:Class="PizzaApp.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PizzaApp"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">
    <Grid Background="White">

        <DockPanel>
            <Label Content="Ваша история заказов:" Margin="10" FontSize="18" FontWeight="SemiBold"/>
        </DockPanel>
        <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="800">
            <ItemsControl ItemsSource="{Binding OrdersItems}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" CornerRadius="10" Padding="15" Margin="10" BorderBrush="LightBlue" BorderThickness="1">


                            <StackPanel>
                                <TextBlock Text="{Binding Id, StringFormat = Заказ№: {0}}" FontSize="18" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding DateOrder, StringFormat=dd.MM.yyyy HH:mm}" FontSize="16" FontWeight="Bold" Foreground="#FF3C3C3C" Margin="0,5,0,0"/>
                                <TextBlock Text="{Binding Status}" FontSize="20" FontWeight="UltraBold" Foreground="{Binding StatBrush}" Margin="0,5,0,0"/>
                                <TextBlock Text="{Binding TotalPrice, StringFormat=Сумма: {0} ₽}" FontSize="16" FontWeight="Bold" Foreground="#FF3C3C3C" Margin="0,5,0,0"/>
                            </StackPanel>

                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

    </Grid>

    <UserControl.Style>
        <Style TargetType="UserControl">
            <Style.Triggers>
                <Trigger Property="Visibility" Value="Visible">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                             From="0.0" To="1.0" Duration="0:0:0.3"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Style>

</UserControl>
